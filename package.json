{
  "name": "memory-os-openclaw-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Memory OS plugin for OpenClaw (fast lane + async lifecycle stubs)",
  "peerDependencies": {
    "openclaw": ">=2026.3.1"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "rehearsal:smoke": "bash scripts/rehearsal-smoke.sh",
    "server": "node src/server/index.js",
    "db:migrate": "node scripts/db-migrate.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.3"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}